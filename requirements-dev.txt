# Development-only dependencies for the accelerator's pure unit tests.
# Runtime (Spark, dbutils, JDBC drivers) is provided by Databricks and is not
# installed here. Only the pure query-building / policy modules are tested.
pytest>=7.0
PyYAML>=6.0
